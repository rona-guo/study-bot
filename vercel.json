{
  "version": 2,
  "builds": [
    {
      "src": "server/api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/v1/(.*)",
      "dest": "server/api/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "public/$1"
    }
  ]
}
